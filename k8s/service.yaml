apiVersion: v1
kind: Service
metadata:
  name: isp-cms-be
  labels:
    app.kubernetes.io/name: isp-cms-be
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: isp-cms-be
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
